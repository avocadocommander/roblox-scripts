// Vision indicator logic has been moved into user-ui-block.client.ts.
// The eye widget is rendered inline in the player name row and is driven by
// the ViewsUpdated remote + the IsStealthing player attribute.
